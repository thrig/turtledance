turtledance.

Necessary ingredients includes a C compiler, make, ucblogo, a source of
data--/dev/random, code libraries, xxd on random system binaries, etc--
place this data into the "input" file--and time to fiddle around with
the inputs and various parameters in the C code. Also, an image capture
program such as scrot(1) and an image viewer such as feh(1) may be of
some utility.
